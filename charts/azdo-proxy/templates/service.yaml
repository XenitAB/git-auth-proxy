apiVersion: v1
kind: Service
metadata:
  name: {{ include "azdo-proxy.fullname" . }}
  labels:
    {{- include "azdo-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azdo-proxy.selectorLabels" . | nindent 4 }}
